FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir \
    fastapi>=0.109 uvicorn[standard]>=0.27 httpx>=0.27 pydantic>=2.5

COPY shared /shared
RUN pip install --no-cache-dir /shared

EXPOSE 8002

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8002", "--reload"]
